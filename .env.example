# LiveKit SIP Docker Compose Environment Variables
# Copy this file to .env and update with your actual values

# LiveKit Server Configuration
LIVEKIT_URL=wss://your-livekit-server.com
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Twilio Configuration (Required for SIP trunk creation)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+13074606119

# Redis Configuration (Optional - uses defaults if not set)
REDIS_PASSWORD=

# Optional: Public IP for SIP (auto-detected if not set)
# PUBLIC_IP=your.public.ip.address

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update LIVEKIT_URL with your LiveKit server WebSocket URL
# 3. Update LIVEKIT_API_KEY and LIVEKIT_API_SECRET from your LiveKit dashboard
# 4. Update TWILIO_ACCOUNT_SID and TWILIO_AUTH_TOKEN from Twilio console
# 5. Run: docker-compose up -d
# 
# The sip-init service will automatically create the trunk and dispatch rule
# for your Twilio number (+13074606119) when the services start.